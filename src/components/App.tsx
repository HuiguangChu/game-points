import React, { memo } from 'react';
import styles from './App.scss';

const App = memo(() => {
    return (
        <div className={styles.appWrapper}>
            start of project
        </div>
    );
});

export default App;
